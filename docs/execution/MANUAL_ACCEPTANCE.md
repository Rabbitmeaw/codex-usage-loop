# B03 真实环境验收矩阵

## 环境记录

| 项目 | 值 |
| --- | --- |
| 记录日期 | 2026-07-22 |
| 构建主机 | Apple M3，macOS 15.7.4（24G517） |
| 构建方式 | `zsh scripts/build-app.sh` |
| 自动测试 | `swift test`，19 项通过 |
| 自动化限制 | 当前执行环境无法可靠读取或驱动桌面应用窗口，因此以下交互项必须在实际桌面会话中确认。 |

## 验收项

| 场景 | 操作 | 预期 | 记录 | 状态 |
| --- | --- | --- | --- | --- |
| 单显示器／围绕 pet | 显示宠物，选择“围绕 pet”与“始终显示用量卡片” | 圆环跟随宠物，卡片紧贴圆环右侧且不遮挡宠物 | 用户确认 | 已验收 |
| 单显示器／左右侧 | 分别选择左侧和右侧圆环 | 卡片位于圆环正下方，屏幕边缘不溢出 | 用户确认 | 已验收 |
| 双显示器 | 将宠物移动到非主显示器 | 圆环和卡片位于同一显示器且坐标方向正确 | 用户确认 | 已验收 |
| 屏幕录制已授权 | 授权后移动／缩放宠物，点击“重新检测宠物位置/大小” | 圆环贴合实际宠物边界 | 用户确认 | 已验收 |
| 屏幕录制未授权 | 撤销授权后启动或重新检测 | 不弹出授权对话框，回退到估算几何 | 用户确认 | 已验收 |
| 原色／单色图标 | 分别切换图标模式并切换系统外观 | 原色保留蓝绿；单色自动保证黑白对比 | 用户确认 | 已验收 |
| 真实双环 | 在账号返回两个窗口时打开菜单 | 显示真实双环，“演示双环”不可用 | 用户确认 | 已验收 |
| 常驻与隐藏宠物 | 启用常驻后隐藏宠物 | 悬浮层保留在左下角，不出现鼠标右键副作用 | 用户确认 | 已验收 |
| 能耗 | 静止 5 分钟，查看活动监视器“能耗” | 无连续高能耗；截图仅在几何变化或 5 秒退避后重试 | 2026-07-23：定位 stdout／stderr EOF 回调忙等；修复后实例连续 5 分 53 秒累计 CPU 时间 2.90 秒、当前 0.0% CPU | 通过 |

## 通过门槛

- 每项写入日期、Codex Desktop 版本、结果与截图或可复现说明。
- 能耗项记录空闲 5 分钟后的活动监视器截图或数值，并与旧版截图中的异常能耗进行对比。
- 若多显示器或权限项失败，不得将 B03 标记为完成；先记录显示器布局和权限状态，再修复。
