# 变更记录

本项目遵循 Keep a Changelog 的简化格式。

## Unreleased

### 已新增

- 菜单增加“在浏览器查看 GitHub Releases…”；仅由用户点击后打开默认浏览器，应用自身不检查、下载或安装更新。
- 新增 Windows 10 22H2 / Windows 11 x64 原生伴随应用，使用 .NET 10 与
  Win32 分层窗口实现圆环、用量卡片、通知区域菜单、点击穿透和自由拖动。
- 新增独立的 Windows 核心模型、额度解析和几何测试，以及 app-server、
  分层窗口和正常退出的生命周期集成测试。
- CI 新增 Windows 构建、测试和单文件发布验证，同时保留 macOS 测试与
  Release 构建。
- 新增 [Windows 构建、运行与限制](docs/WINDOWS.md)，说明 DPI、依赖、
  打包、诊断和系统 DLL。

### 已修复

- 排除 `ChatGPT Computer Use / Software Cursor` 等辅助窗口，避免圆环突然使用错误的小窗口几何。
- “立即刷新”不再同时触发 pet 重测，并移除与前台应用冲突的 ⌘R；刷新中及保留旧数据时的失败状态现在可见。

### 已知限制

- Windows 版本当前仅提供 x64、依赖 .NET 10 Runtime 的单文件发布。
- Windows 商店版 Codex GUI 的内置 CLI 没有外部执行入口；实时额度需要独立
  安装官方 Codex CLI 或设置 `CODEX_EXECUTABLE`。
- 混合 DPI 多显示器定位仍需补充更多拓扑测试。

## 0.1.1 — 2026-07-24

### 已新增

- 为宠物窗口候选评分、宠物估算位置、CG→AppKit 坐标换算、圆环位置与像素宠物定位新增可复现单元测试。
- 新增 [用户手册](docs/MANUAL.md)，集中说明安装、权限、菜单与故障排除。
- 新增真实桌面环境的[手工验收矩阵](docs/execution/MANUAL_ACCEPTANCE.md)。
- 可分别自定义内环和外环颜色，并恢复默认蓝绿；颜色以 sRGB 保存。
- 新增可复现的本地 Release 打包、校验和和构建元数据生成脚本。
- 新增 MIT License，并将 README 改为顶部可点击切换的中文／English 双语版本。
- 发布公开 GitHub 仓库和首个 `v0.1.0` Release，提供应用 ZIP、SHA-256 与构建元数据。
- 屏幕录制授权后的实测 pet 像素边界定位：围绕 pet 时，圆环圆心和直径直接来自可见 pet 边界。
- README 的中英双语功能与使用说明，以及扩展的中文用户手册：覆盖布局、常驻卡片、颜色、图标、启动 companion、刷新、双环和权限流程。
- pet 隐藏时仍展示用量圆环；“始终显示用量卡片”现在仅决定详情卡是否显示。
- 新增面向维护者的 [Release 发布流程](docs/RELEASING.md)，将 README 的用户下载安全提示与发布工件命令分层。

### 已变更

- 将定位与坐标公式抽取为纯几何函数；实际窗口筛选、截图授权判断、缓存和估算回退策略保持不变。
- 收敛截图定位器的异步同步边界，消除 Swift 6 并发迁移警告。
- EOF 时移除 app-server stdout／stderr 管道回调，修复持续占用一个 CPU 核的忙等问题。
- 明确 macOS 13 的窗口几何估算与 macOS 14+ 的像素级定位能力边界。
- 像素级定位改为首次定位或宠物容器／布局变化时触发；成功测量会缓存，稳态不持续截图。
- 屏幕录制仅在用户从菜单选择“启用像素级定位…”后才请求，避免启动时打断 macOS 的授权、退出与重新打开流程。
- 将围绕 pet 的圆环画布修正为完整 SwiftUI 画布，消除圆心视觉偏移；左右屏幕边缘定位已完成验收。

## 0.1.0 — 2026-07-22

### 已新增

- CodexUsageLoop：跟随 Codex 宠物显示 5 小时和 7 天用量圆环。
- 常驻用量卡片、真实双环优先、自由拖动、重新检测、立即刷新。
- 蓝绿原色与随系统的单色菜单栏图标。

### 已变更

- 移除全局右键监听，避免对其他应用造成副作用。
- 将定位轮询降为 1 秒，并加入截图退避、结果缓存和候选窗口优先筛选以降低能耗。
