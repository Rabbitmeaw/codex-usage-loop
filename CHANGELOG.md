# 变更记录

本项目遵循 Keep a Changelog 的简化格式。

## Unreleased

### 已新增

- 为宠物窗口候选评分、宠物估算位置、CG→AppKit 坐标换算、圆环位置与像素宠物定位新增可复现单元测试。
- 新增 [用户手册](docs/MANUAL.md)，集中说明安装、权限、菜单与故障排除。
- 新增真实桌面环境的[手工验收矩阵](docs/execution/MANUAL_ACCEPTANCE.md)。
- 可分别自定义内环和外环颜色，并恢复默认蓝绿；颜色以 sRGB 保存。
- 新增可复现的本地 Release 打包、校验和和构建元数据生成脚本。
- 新增 MIT License，并将 README 改为顶部可点击切换的中文／English 双语版本。
- 发布公开 GitHub 仓库和首个 `v0.1.0` Release，提供应用 ZIP、SHA-256 与构建元数据。

### 已变更

- 将定位与坐标公式抽取为纯几何函数；实际窗口筛选、截图授权判断、缓存和估算回退策略保持不变。
- 收敛截图定位器的异步同步边界，消除 Swift 6 并发迁移警告。
- EOF 时移除 app-server stdout／stderr 管道回调，修复持续占用一个 CPU 核的忙等问题。
- 明确 macOS 13 的窗口几何估算与 macOS 14+ 的像素级定位能力边界。

## 0.1.0 — 2026-07-22

### 已新增

- CodexUsageLoop：跟随 Codex 宠物显示 5 小时和 7 天用量圆环。
- 常驻用量卡片、真实双环优先、自由拖动、重新检测、立即刷新。
- 蓝绿原色与随系统的单色菜单栏图标。

### 已变更

- 移除全局右键监听，避免对其他应用造成副作用。
- 将定位轮询降为 1 秒，并加入截图退避、结果缓存和候选窗口优先筛选以降低能耗。
