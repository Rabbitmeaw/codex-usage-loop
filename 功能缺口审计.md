# CodexUsageLoop｜功能缺口审计

审计日期：2026-07-22

## 结论

现有菜单栏功能均有实际调用链：常驻卡片、自由拖动、圆环位置、双环演示、立即刷新、重新检测和退出都不是占位项。

尚未完成的内容主要是质量保障、兼容性和发布能力，而不是缺少菜单功能。

## 已写明但尚未实现

| 项目 | 状态 | 依据 |
| --- | --- | --- |
| app-server JSON-RPC 解析的可注入单元与边界测试 | 未实现 | 路线图 B01 仍为“待开始”；当前测试只覆盖模型、布局、双环互斥和截图退避。 |
| 宠物窗口筛选、坐标换算与像素定位的可复现测试样例 | 未实现 | 路线图 B02 仍为“待开始”。 |
| 单屏、多屏、不同缩放、宠物位置和权限状态的人工验收矩阵 | 未实现 | 路线图 B03 仍为“待开始”。 |
| macOS 13 的精确定位替代方案或最低版本调整 | 未决 | 精确路径依赖 macOS 14 的 ScreenCaptureKit；文档仍声明 macOS 13+。 |
| Developer ID 签名和 Apple 公证 | 未实现 | 当前仅使用 ad-hoc 本地签名，不能作为对外分发方案。 |

## 文档承诺与代码不一致

| README 表述 | 实际代码 | 应处理方式 |
| --- | --- | --- |
| 宠物位置每 0.15 秒检查 | 当前为每 1 秒检查，作为能耗优化的一部分 | 更新 README。 |
| 默认卡片显示在宠物下方 | 围绕 pet 时卡片固定在圆环右侧；左右圆环时在下方 | 更新 README。 |
| 首次运行会请求屏幕录制权限 | 代码只使用 `CGPreflightScreenCaptureAccess()` 检查，刻意不主动弹出授权请求 | 改为“需要用户先在系统设置授予权限”。 |

## 已实现但仍需真实验证

- 菜单栏原色／单色模式已经实现并持久化，但尚未对浅色与深色菜单栏进行人工视觉验收。
- 截图退避、候选窗口收敛与减少面板更新已实现并有策略测试，但尚未在活动监视器中获得新的长期能耗样本。
- 宠物位置重校准和 app-server 刷新已有调用链，但需要真实 Codex Desktop 版本回归验证。

## 已知技术债

- release 构建仍有 Swift 并发迁移警告：`ScreenCaptureMascotLocator` 在异步上下文使用 `NSLock`，并捕获非 `Sendable` 实例。当前可构建，但未来 Swift 6 严格模式可能将其升级为错误。

## 建议优先级

1. 先完成 B03 人工验收矩阵，验证图标模式、卡片位置和实际能耗。
2. 处理 README 三处失真表述。
3. 完成 B01 和 B02，为协议变更及定位回归建立测试安全网。
4. 决定 macOS 13 支持策略与是否对外分发。
