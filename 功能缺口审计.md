# CodexUsageLoop｜功能缺口审计

更新日期：2026-07-25

本文件记录当前差距，不再作为路线图。批次状态以
[`docs/execution/PROGRESS.md`](docs/execution/PROGRESS.md) 为唯一权威来源。

## 结论

macOS 与 Windows 的核心菜单能力已有实际调用链：用量刷新、pet 重测、常驻
卡片、自由拖动、圆环位置、外观设置和退出均不是占位项。早期审计中的 parser
测试、定位样例、B03 真实环境验收、Git／Release 与 Windows 版本均已完成。

## 已关闭的旧缺口

| 旧问题 | 当前状态 |
| --- | --- |
| app-server 解析缺少边界测试 | 已由纯 parser fixtures 覆盖 |
| 窗口筛选、坐标换算与像素定位无样例 | 已有可复现回归测试 |
| B03 未开始 | 已完成；历史记录见手工验收矩阵 |
| macOS 13 支持未决 | 产品目标已由 D-018 收口；菜单门禁与状态误报实现缺口转入 B38 |
| Codex Desktop 支持范围未决 | 已由 D-019 收口为 Release 说明记录的已回归稳定版本和能力契约 |
| 目录无 Git／无公开发布 | 已建立 Git 仓库、MIT License 与官方 Releases |

## 当前验证边界

| 项目 | 当前边界 | 发布要求 |
| --- | --- | --- |
| macOS 13 定位 | 捕获路径会降级，但菜单仍可请求并可能误报像素能力 | B38 增加门禁测试与修复，再执行实机 smoke |
| macOS 14+ 精确定位 | 用户主动授权后才使用 ScreenCaptureKit | 覆盖授权／拒绝／降级路径 |
| Windows 原生交互 | macOS 不能执行 Win32 消息循环和托盘实测 | Windows CI／实机运行集成测试 |
| Codex app-server | 实验性能力契约可能随稳定版变化 | 发布前采集真实响应并运行 parser fixtures |
| 混合 DPI 多屏 | 已有核心几何与部分实机证据，拓扑仍可能扩展 | 在可用 Windows 实机持续补充 smoke |
| macOS 分发身份 | 无 Developer ID 签名或 Apple 公证 | 继续提供校验和、tag 与提交追溯 |

## 当前优先级

1. 先完成 B38，确保 macOS 13 不暴露不可用的像素入口且状态准确。
2. 每次发布执行真实 app-server、窗口／状态定位和可用平台实机 smoke。
3. 在 Windows CI／实机执行原生集成测试，不能用跨平台 Core 测试替代。
4. 保持错误可见且不伪造数据；兼容失败时优先提供明确诊断。
