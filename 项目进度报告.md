# CodexUsageLoop for macOS｜项目进度报告

更新于：2026-07-22

## 当前结论

项目已经具备可构建、可打包的 macOS 伴随层原型。核心产品闭环已实现：通过本机 Codex app-server 获取用量、定位 Codex 或 ChatGPT 宠物窗口、在宠物附近绘制用量圆环，并以菜单栏提供展示与位置控制。

本次检查中，Swift 单元测试 2 项全部通过，release 构建通过；现有打包产物的签名完整性校验通过。

## 已完成能力

- macOS 13+ SwiftPM 原生应用，使用 AppKit 与 SwiftUI。
- 通过 `codex app-server --stdio` 读取 `account/rateLimits/read`，解析短周期与周周期剩余用量、重置时间。
- 默认跟随宠物；可切换始终显示、自由拖动，以及圆环围绕宠物、左侧、右侧三种位置。
- 当同时存在两类额度时绘制双环；菜单支持本地双环演示。
- 鼠标悬停圆环显示详情卡，右键圆环可弹出控制菜单。
- 使用 CGWindowList 筛选 Codex 或 ChatGPT 的小型窗口；读取 Codex 本地状态作为宠物窗口候选排序和布局提示。
- 已实现可选的 ScreenCaptureKit 图像定位，用于从透明覆盖窗口中进一步估计宠物的实际边界；没有屏幕录制权限时回退至窗口比例估算。
- 构建脚本生成 `dist/CodexUsageLoop.app`、转换图标并使用本地临时签名。

## 实现结构

| 区域 | 主要文件 | 职责 |
| --- | --- | --- |
| 应用编排 | `Sources/CodexPetUsageMac/AppController.swift` | 生命周期、悬浮窗、菜单、定时刷新和窗口位置计算 |
| 用量数据 | `Sources/CodexPetUsageMac/CodexAppServerClient.swift` | 启动 app-server、JSON-RPC 通信与限额解析 |
| 宠物定位 | `Sources/CodexPetUsageMac/PetWindowLocator.swift` | 候选窗口筛选、显示器匹配与回退几何 |
| 画面细化 | `Sources/CodexPetUsageMac/ScreenCaptureMascotLocator.swift` | ScreenCaptureKit 截图与像素连通区域识别 |
| 界面状态与视图 | `Sources/CodexPetUsageMac/Models.swift`、`UsageView.swift` | 持久化偏好、圆环与详情卡 |
| 构建与交付 | `scripts/build-app.sh`、`Resources/AppIcon.svg` | 打包、图标和临时签名 |

## 验证记录

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 单元测试 | 通过 | `swift test`：2 项通过，0 失败 |
| release 编译 | 通过 | `swift build -c release` 完成 |
| 打包产物 | 已存在 | `dist/CodexUsageLoop.app`，版本 0.1.0 |
| 签名完整性 | 通过 | `codesign --verify --deep --strict` 通过 |

## 当前局限与后续重点

1. 尚未进行真实 Codex Desktop 环境下的端到端人工验证，尤其是不同宠物位置、多显示器、缩放比例和屏幕录制授权状态。
2. 自动化测试仅覆盖 `UsageWindow` 与默认偏好，未覆盖 JSON-RPC 限额解析、窗口候选排序、坐标换算、像素检测和菜单交互。
3. 用量接口依赖实验性的 app-server 协议；Codex 更新后可能变更字段或方法名。
4. 定位逻辑依赖窗口所有者名称、尺寸范围及 `~/.codex/.codex-global-state.json` 的非稳定字段，兼容性需要随 Codex Desktop 版本回归验证。
5. 现有应用采用 ad-hoc 本地签名，适合本机运行；若要分发给其他用户，还需要 Developer ID 签名与公证流程。
6. 当前目录不是 Git 仓库，无法从提交历史、分支或工作区差异判断需求演进和未提交改动。

## 建议的下一阶段

优先进行一次真实环境验收并记录结果：打开 Codex 宠物、分别切换宠物位置和窗口大小、授予或撤销屏幕录制权限、验证双环和悬停详情卡。随后针对已确认的实际行为补充解析与定位的单元测试，再处理版本兼容和分发需求。
