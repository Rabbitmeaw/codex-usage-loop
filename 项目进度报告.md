# CodexUsageLoop｜项目进度报告

更新于：2026-07-25

本报告是当前快照。详细批次、验收证据与后续工作以
[`docs/execution/PROGRESS.md`](docs/execution/PROGRESS.md) 为唯一权威来源。

## 当前结论

项目已形成可构建、可验证的跨平台本地伴随应用：

- macOS 13+ 使用 SwiftPM、AppKit 与 SwiftUI；macOS 14+ 可选
  ScreenCaptureKit 像素级定位。
- Windows 10 22H2／Windows 11 x64 使用 .NET 10 与原生 Win32 分层窗口。
- 两个平台都从本机 `codex app-server --stdio` 获取用量，无自有服务端、
  数据库或应用内更新请求。
- macOS 自动化基线为 52 项 Swift 测试；Windows Core 基线为 29 项检查。

## 已完成能力

- 5 小时／7 天用量的单环、双环与详情卡。
- pet 跟随、围绕／左／右布局、自由拖动、常驻卡片和独立几何重测。
- 刷新中、首次失败及保留旧数据时失败反馈。
- macOS 可选像素级定位、颜色与菜单栏图标偏好、登录 companion。
- Windows Per-Monitor V2 DPI、透明分层窗口、通知区域菜单和本地设置。
- parser、窗口候选、坐标／几何、像素定位和展示状态自动化。
- MIT 开源 Release、SHA-256 与构建元数据；用户主动打开官方 Releases。

## 兼容与隐私契约

- macOS 13 的兼容目标只承诺窗口几何估算、明确状态和可重测，不承诺像素级
  精度；当前菜单门禁与授权状态误报仍待 B38。
- 不维护 Codex Desktop／CLI 历史版本矩阵；每个 Release 只承诺其发布说明记录
  的已回归稳定版本与能力契约，并回归真实 app-server、parser fixtures 与定位
  路径。
- 应用不读取认证文件，不上传用量、窗口信息或屏幕画面，不主动联网检查更新。
- app-server 或定位契约不兼容时显示明确错误，不伪造数据。

## 发布验证边界

| 项目 | 当前证据 | 仍需的发布门槛 |
| --- | --- | --- |
| macOS 逻辑 | 52 项 Swift 测试、Release 构建与严格签名校验 | B38 菜单门禁修复与 macOS 13 实机估算 smoke |
| Windows Core | 29 项检查、Release 交叉构建／单文件发布 | Windows CI／实机原生窗口集成 |
| Codex 兼容 | parser fixtures 与既有真实环境验收 | 记录确切稳定版本并完成真实契约回归 |

项目目录已是 Git 仓库并存在公开 GitHub Release。早期报告中的“无 Git”、
“2 项测试”、“右键圆环菜单”和“B03 待完成”等描述均已失效。
